import 'package:flutter/material.dart';

class MailScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: new AppBar(
        title: new Text('Mail'),
      ),
      body: new Center(
        child: new Text("Mail center"),
      ),
    );
  }
}
